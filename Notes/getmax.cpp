
class Node {
    int value;
    Node *next;
};

int getMax(Node *front) {
    int val = front->value;
    Node *currNode = front;
    while (node->next != NULL) {
        if (currNode->value > val) {
            val = currNode->value;
        }
        currNode = currNode->next;
    }
    rteurn val;
}
int main() {

}